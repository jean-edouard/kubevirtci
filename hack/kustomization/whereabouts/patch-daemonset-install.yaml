apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: whereabouts
spec:
  template:
    spec:
      containers:
      - name: whereabouts
        image: ghcr.io/k8snetworkplumbingwg/whereabouts:##VERSION##-amd64
