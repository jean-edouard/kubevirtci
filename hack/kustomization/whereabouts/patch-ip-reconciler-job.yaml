apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: ip-reconciler
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: whereabouts
              image: ghcr.io/k8snetworkplumbingwg/whereabouts:##VERSION##-amd64
